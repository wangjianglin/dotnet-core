<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Lin.Core.Controls.NotifierControls">

    <Style TargetType="{x:Type local:NetWorkExceptionControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:NetWorkExceptionControl}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80" />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0"
                                Width="80"
                                Height="65"
                                Margin="0,0,0,20">
                            <Border.Background>
                                <ImageBrush ImageSource="/Lin.Core;component/Images/RibbonView/3.png" />
                            </Border.Background>
                        </Border>
                        <TextBlock Grid.Column="1"
                                   Margin="0,0,0,20"
                                   VerticalAlignment="Center"
                                   Text="与服务器断开连接" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>